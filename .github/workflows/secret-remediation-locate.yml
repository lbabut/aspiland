name: Secret remediation locate

on:
  pull_request:
    paths:
      - ".github/workflows/secret-remediation-locate.yml"
      - "scripts/run_secret_snapshot_scan.py"
      - "scripts/audit-remediation-public.pem"
  workflow_dispatch:

permissions:
  contents: read

jobs:
  locate:
    name: locate-2025-04-20-snapshots
    runs-on: ubuntu-latest
    timeout-minutes: 25
    steps:
      - name: Checkout complete Git history
        uses: actions/checkout@v6
        with:
          fetch-depth: 0
          show-progress: false

      - name: Scan every commit snapshot from 2025-04-20
        run: >-
          python3 scripts/run_secret_snapshot_scan.py
          --period "2025-04-20-snapshots"
          --since "2025-04-20T00:00:00Z"
          --until "2025-04-21T00:00:00Z"
          --summary "${RUNNER_TEMP}/safe-secret-summary/2025-04-20-snapshots.json"
          --restricted-locations "${RUNNER_TEMP}/restricted-secret-locations/2025-04-20-snapshots.json"

      - name: Encrypt remediation locations when needed
        shell: bash
        run: |
          set -euo pipefail
          summary="${RUNNER_TEMP}/safe-secret-summary/2025-04-20-snapshots.json"
          status="$(python3 -c 'import json,sys; print(json.load(open(sys.argv[1]))["status"])' "$summary")"
          if [[ "$status" != "findings" ]]; then
            rm -f "${RUNNER_TEMP}/restricted-secret-locations/2025-04-20-snapshots.json"
            exit 0
          fi

          output="${RUNNER_TEMP}/encrypted-remediation-2025-04-20-snapshots"
          mkdir -p "$output"
          openssl rand -out "${RUNNER_TEMP}/audit-key.bin" 32
          openssl enc -aes-256-cbc -salt -pbkdf2 \
            -pass file:"${RUNNER_TEMP}/audit-key.bin" \
            -in "${RUNNER_TEMP}/restricted-secret-locations/2025-04-20-snapshots.json" \
            -out "$output/locations.enc"
          openssl pkeyutl -encrypt -pubin \
            -inkey scripts/audit-remediation-public.pem \
            -pkeyopt rsa_padding_mode:oaep \
            -pkeyopt rsa_oaep_md:sha256 \
            -in "${RUNNER_TEMP}/audit-key.bin" \
            -out "$output/key.enc"
          rm -f "${RUNNER_TEMP}/restricted-secret-locations/2025-04-20-snapshots.json" \
            "${RUNNER_TEMP}/audit-key.bin"

      - name: Upload encrypted remediation map when present
        uses: actions/upload-artifact@v4
        with:
          name: encrypted-remediation-2025-04-20-snapshots
          path: ${{ runner.temp }}/encrypted-remediation-2025-04-20-snapshots/
          if-no-files-found: ignore
          retention-days: 1

      - name: Upload sanitized summary
        uses: actions/upload-artifact@v4
        with:
          name: safe-secret-summary-2025-04-20-snapshots
          path: ${{ runner.temp }}/safe-secret-summary/2025-04-20-snapshots.json
          if-no-files-found: error
          retention-days: 1

      - name: Report sanitized result
        shell: bash
        run: |
          python3 - "${RUNNER_TEMP}/safe-secret-summary/2025-04-20-snapshots.json" <<'PY'
          import json
          import sys

          with open(sys.argv[1], encoding="utf-8") as handle:
              result = json.load(handle)
          print(
              f"Sanitized snapshot result: status={result['status']}, "
              f"commits_scanned={result['commits_scanned']}, "
              f"suspected_findings={result['suspected_findings']}"
          )
          if result["status"] == "scanner-error":
              raise SystemExit(1)
          PY
